/** @type {import('next').NextConfig} */
const nextConfig = {
  // Decorative brand imagery ships from /public with CSS-controlled aspect
  // ratios; plain <img> keeps the 1:1 port faithful (no next/image sizing).
};

export default nextConfig;
